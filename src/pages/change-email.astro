---
import Layout from "../layouts/Layout.astro";
---

<Layout title="تغيير الإيميل">
  <section class="max-w-md mx-auto mt-16 bg-white shadow-lg rounded-2xl p-6">
    <h1 class="text-2xl font-bold text-center mb-4">تغيير الإيميل 📧</h1>
    <p class="text-gray-600 text-center mb-4">
      غادي نرسل ليك رابط تأكيد للإيميل الجديد باش نتحقق أنه ديالك.
    </p>

    <form method="post" action="/api/request-email-change" class="space-y-4">
      <div>
        <label class="block mb-1">الإيميل الجديد:</label>
        <input
          type="email"
          name="newEmail"
          required
          class="w-full border rounded-lg px-3 py-2 focus:outline-none focus:ring-2 focus:ring-purple-500"
        />
      </div>

      <button
        type="submit"
        class="w-full bg-purple-600 hover:bg-purple-700 text-white font-semibold py-2 rounded-lg"
      >
        إرسال رابط التأكيد 🚀
      </button>
    </form>

    <p class="mt-4 text-xs text-gray-500 text-center">
      من بعد ما توصلك الرسالة، ضغط على الرابط باش يتبدّل الإيميل نهائياً.
    </p>
  </section>
</Layout>

---
import Layout from "../layouts/Layout.astro";

const { searchParams } = Astro.url;
const token = searchParams.get("token") || "";
---

<Layout title="إعادة تعيين كلمة المرور">
  <section class="max-w-md mx-auto mt-16 bg-white shadow-lg rounded-2xl p-6">
    <h1 class="text-2xl font-bold text-center mb-2">إعادة تعيين كلمة المرور 🔐</h1>

    {token === "" ? (
      <p class="text-center text-red-500">الرابط غير صالح.</p>
    ) : (
      <form method="post" action="/api/reset-password" class="space-y-4">
        <input type="hidden" name="token" value={token} />

        <div>
          <label class="block mb-1">كلمة المرور الجديدة:</label>
          <input
            type="password"
            name="password"
            required
            minlength="6"
            class="w-full border rounded-lg px-3 py-2 focus:outline-none focus:ring-2 focus:ring-purple-500"
          />
        </div>

        <div>
          <label class="block mb-1">تأكيد كلمة المرور:</label>
          <input
            type="password"
            name="confirm"
            required
            minlength="6"
            class="w-full border rounded-lg px-3 py-2 focus:outline-none focus:ring-2 focus:ring-purple-500"
          />
        </div>

        <button
          type="submit"
          class="w-full bg-purple-600 hover:bg-purple-700 text-white font-semibold py-2 rounded-lg"
        >
          حفظ كلمة المرور الجديدة ✅
        </button>
      </form>
    )}
  </section>
</Layout>
